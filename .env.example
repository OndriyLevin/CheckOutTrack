# --- SERVER CONFIGURATION ---
# Rename this file to .env on your server (same directory as docker-compose.deploy.yml)

# 1. Telegram Admin ID
# Get this from @userinfobot or similar.
# Users with this ID will have access to the Admin Dashboard.
ADMIN_ID=123456789

# 2. Database URL
# Path to the SQLite database file inside the container.
# Ensure the volume is mounted correctly in docker-compose.deploy.yml
DATABASE_URL="file:/app/data/prod.db"

# 3. Port (Optional)
# Internal port for the Node.js server. Default is 3000.
PORT=3000
